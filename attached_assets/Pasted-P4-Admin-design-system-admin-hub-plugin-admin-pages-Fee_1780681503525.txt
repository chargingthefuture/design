P4 — Admin design system + admin hub + plugin admin pages + Feedback UI ✅ ready (owner‑approved)

Resolved 2026‑06‑04: the owner wants this — one unified admin hub you navigate to, then click through to each plugin's admin page (more condensed than v2's per‑plugin nav pages). Directory is intentionally excluded (inline — guardrail 1) and is already not in the 14‑list below.
PART A — unified AdminShell.tsx chrome + reusable kit (data table, filter bar, status badges, detail drawer, confirm dialog, pagination, toast, empty/loading). PART B — AdminHub.tsx landing (grid of admin‑area cards with at‑a‑glance counts) (+ Loading/Empty). PART C — 14 plugin admin templates (Loading/Empty/Populated each): Feed+Announcements, Foundation, GDP, LevelUp, LightHouse, Peer Programming, Service Credits, Skills Hunt, SocketRelay, TrustTransport, Unlock, Weekly Performance, Workforce, WhatWorks. Directory NOT included (inline). PART D — WhatWorks admin detail (accent #84CC16): moderation queue (approve/reject‑with‑reason/delete + status filter) and problem curation (create/activate/deactivate/edit/delete) → WhatWorksAdmin.tsx (+ Empty/Loading). PART E — Feedback "PM agent": E1 user‑facing (+ Mobile) FeedbackSubmit (+success), FeedbackBoard (+Empty/Loading), FeedbackStatus; E2 admin (in the hub) FeedbackAdminInbox, FeedbackAdminDetail (matcher result + confidence meter + proposed schema/API/command changes), FeedbackApprovalQueue (approve/reject‑reason/modify — the centerpiece), FeedbackImplementationMonitor (read‑only run log). Dark tokens: bg #0F1117, panel #0D0F14, far rail #090B0F, surface #161B27, border #1E2A3A, text #F9FAFB, subtle #6B7280; per‑app accents; neutral #6366F1 for cross‑cutting admin/Feedback. Admin surfaces: Loading/Empty/Populated, no public state. Author in artifacts/mockup-sandbox/..., // design-sync line 1, add each to sync-manifest.json, never use the word "phase". Keep copy calm, plain, trauma‑informed ("survivors", never "victims"). Design only what the backend supports.