// design-sync
// Genuinely zero profiles — no filter/search active, no Browse All button
import { Directory } from "./Directory";
export function DirectoryEmpty() {
  return <Directory genuinelyEmpty />;
}
